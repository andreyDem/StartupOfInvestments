# CREATE
INSERT INTO users  (NAME, SURNAME, ADDRESS, PHONE, ROLE) VALUES (?, ?, ?, ?, ?);

# FIND_BY_ID
SELECT USERS.NAME, USERS.SURNAME, USERS.ADDRESS, USERS.PHONE, USERS.ROLE
FROM  USERS
WHERE USERS.ID = ?;

# FIND_BY_NAME
SELECT NAME, SURNAME, ADDRESS, PHONE, ROLE
FROM  USERS
WHERE users.NAME = ?;

# UPDATE
UPDATE users SET NAME = ?, SURNAME = ?, ADDRESS = ?, PHONE = ?, ROLE = ? WHERE ID = ?;

# DELETE
DELETE FROM users WHERE ID = ?;

# FIND_ALL
SELECT * FROM users;

# GET_LAST_INSERTED
SELECT LAST_INSERT_ID()