# CREATE
INSERT INTO INVESTMENTS (SUM) VALUE (?);

# FIND_BY_ID
SELECT INVESTMENTS.ID ID_INVESTMENT, INVESTMENTS.SUM SUM_INVESTMENT, ID_PROJECT ID_PROJECT,
  PROJECTS.NAME PROJECT_NAME, FINAL_COST, ASSEMBLE_COST, PROJECTS.NAME, SURNAME
FROM investments INVESTMENTS
  JOIN investments_projects I_P ON INVESTMENTS.ID = I_P.ID_INVESTMENTS
  JOIN projects PROJECTS ON I_P.ID_PROJECT = PROJECTS.ID
  JOIN users USERS ON PROJECTS.ID_USER = USERS.ID
WHERE INVESTMENTS.ID = ?;

# FIND_BY_USER ID
SELECT INVESTMENTS.ID ID_INVESTMENT, INVESTMENTS.SUM SUM_INVESTMENT, ID_PROJECT ID_PROJECT,
  PROJECTS.NAME PROJECT_NAME, FINAL_COST, ASSEMBLE_COST, PROJECTS.NAME, SURNAME
FROM investments INVESTMENTS
  JOIN investments_projects I_P ON INVESTMENTS.ID = I_P.ID_INVESTMENTS
  JOIN projects PROJECTS ON I_P.ID_PROJECT = PROJECTS.ID
  JOIN users USERS ON PROJECTS.ID_USER = USERS.ID
WHERE USERS.ID = ?;

# FIND_BY_USER_NAME
SELECT INVESTMENTS.ID ID_INVESTMENT, INVESTMENTS.SUM SUM_INVESTMENT, ID_PROJECT ID_PROJECT,
  PROJECTS.NAME PROJECT_NAME, FINAL_COST, ASSEMBLE_COST, PROJECTS.NAME, SURNAME
FROM investments INVESTMENTS
  JOIN investments_projects I_P ON INVESTMENTS.ID = I_P.ID_INVESTMENTS
  JOIN projects PROJECTS ON I_P.ID_PROJECT = PROJECTS.ID
  JOIN users USERS ON PROJECTS.ID_USER = USERS.ID
WHERE USERS.NAME = ?;

# FIND_BY_PRICE
SELECT INVESTMENTS.ID ID_INVESTMENT, INVESTMENTS.SUM SUM_INVESTMENT, ID_PROJECT ID_PROJECT,
  PROJECTS.NAME PROJECT_NAME, FINAL_COST, ASSEMBLE_COST, PROJECTS.NAME, SURNAME
FROM investments INVESTMENTS
  JOIN investments_projects I_P ON INVESTMENTS.ID = I_P.ID_INVESTMENTS
  JOIN projects PROJECTS ON I_P.ID_PROJECT = PROJECTS.ID
  JOIN users USERS ON PROJECTS.ID_USER = USERS.ID
WHERE INVESTMENTS.SUM = ?;

# FIND_BY_PROJECT_NAME
SELECT INVESTMENTS.ID ID_INVESTMENT, INVESTMENTS.SUM SUM_INVESTMENT, ID_PROJECT ID_PROJECT,
  PROJECTS.NAME PROJECT_NAME, FINAL_COST, ASSEMBLE_COST, PROJECTS.NAME, SURNAME
FROM investments INVESTMENTS
  JOIN investments_projects I_P ON INVESTMENTS.ID = I_P.ID_INVESTMENTS
  JOIN projects PROJECTS ON I_P.ID_PROJECT = PROJECTS.ID
  JOIN users USERS ON PROJECTS.ID_USER = USERS.ID
WHERE PROJECTS.NAME = ?;

# UPDATE
UPDATE investments SET  SUM = ? WHERE ID = ?;

# DELETE
DELETE FROM investments WHERE ID = ?;

# FIND ALL
SELECT * FROM investments;

# GET_LAST_INSERTED
SELECT LAST_INSERT_ID();